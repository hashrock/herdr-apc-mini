[package]
name = "herdr-apc-mini"
version = "0.1.0"
edition = "2024"

[dependencies]
midir = "0.11.0"
serde_json = "1.0.151"

# CoreMIDI の抜き差し通知を受け取るためだけに使う。midir が中で使っているものと同じ。
[target.'cfg(target_os = "macos")'.dependencies]
coremidi = "0.9.2"

[[bin]]
name = "apcd"
path = "src/main.rs"
